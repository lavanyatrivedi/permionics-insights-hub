{
  "buildCommand": "pnpm --filter @workspace/permionics-bd-suite run build && cp -r artifacts/permionics-bd-suite/dist ./dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
